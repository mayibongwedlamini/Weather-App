<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>

    <!-- CSS Stylesheets -->
    <link rel="stylesheet" href="../styles/main.css">


    <!-- Bootstrap Scripts -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

</head>

<body>
<div class="home-page">
    <!--form to search for city-->
    <div class="search-bar">
        <form action="/" method="POST">
            <input type="search" id="city" name="city" class="form-control " placeholder="Search..."
                aria-label="Search">
        </form>
    </div>

    <% if(weatherData) { %>

        <div class="weather-info">
            <div class="weather-data">
                <!--Data presentation-->

                <h1>
                    <%= weatherData.name %>, <%= weatherData.sys.country %>
                </h1>
                <p>
                    <% weatherData.timezone %>
                </p>
                <p>Day: <%= Math.round(weatherData.main.temp_max - 273.15) %>° &uarr; &#x2B51; Night: <%=
                            Math.round(weatherData.main.temp_min - 273.15) %>° &darr;</p>
                <h2 id="temperature">
                    <%= Math.round(weatherData.main.temp - 273.15) %>°C
                </h2>
                <p> Feels like <%= Math.round(weatherData.main.feels_like - 273.15) %>°</p>
                <p id="weather">
                    <%= weatherData.weather[0].main %>
                </p>



            </div>

            <% } %>

                <!--footer -->
                <div class="footer">
                    <footer class="white-section" id="footer">
                        <div class="container-fluid">
                            <p>© Copyright <%= currentYear %> All Rights Reserved</p>
                        </div>
                    </footer>
                </div>
</div>
</body>

</html>